Не выполняя кода, ответить, что выведет браузер и почему:

if (!("a" in window)) {
    var a = 1;
}
alert(a);
Исходя из предположения, что строка "a" отсутствует в "window", переменной a присвоится единица, и эта единица выведется браузером



var b = function a(x) {
    x && a(--x);
};
alert(a);
Поскольку функция -a- объявлена выражением, она не обладает глобальной областью видимости и alert не выведет ничего.




function a(x) {
    return x * 2;
}
var a;
alert(a);
Поскольку переменная -a- переопределяет функцию -a- и не обладает значением, то alert не выведет ничего.





function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3);
Думаю, что свойство arguments переопределит аргумент -a- (=3), переданный функции на 10, и 10 выведется alert-ом




function a() {
    alert(this);
}
a.call(null);
Может что-то вроде Object function
